{
  "name": "automated-dialer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm --workspace apps/frontend run dev\" \"npm --workspace apps/backend run dev\"",
    "build": "npm --workspace apps/frontend run build && npm --workspace apps/backend run build",
    "start:frontend": "npm --workspace apps/frontend run start",
    "start:backend": "npm --workspace apps/backend run start",
    "lint": "npm --workspace apps/frontend run lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
